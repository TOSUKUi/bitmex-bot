  - bot_id: # input bot_id
    legs_span: # <choose in [1m, 5m, 15m, 30m, 45m, 1h, 4h, 1d]>
    logic: # logic_python_file_name
    trading_platform: # bitmex only now
    api_credential:
      test_net: # if 
        apiKey: # your test net api id
        secret: # your test net api secret
      main_net:
        apiKey: # your api id
        secret: # your api secret
    test: # True or False
  